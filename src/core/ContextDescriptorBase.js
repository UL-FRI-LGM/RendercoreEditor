export class ContextDescriptorBase { //RC context descriptor base

    
    // #context;


    constructor(args = {}) {
        // this.context = null;
    }


    // get context() { return this.#context; }
    // set context(context) { this.#context = context; }
};